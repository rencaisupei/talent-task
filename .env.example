# 本機建置 iOS／Android App 時的環境變數。
#
#   cp .env.example .env
#
# Expo CLI 會自動載入 .env（`expo prebuild`、`expo run:android`、Gradle 打包 JS 時都會）。
# .env 已列在 .gitignore，不會被提交。
#
# 注意：只有 EXPO_PUBLIC_ 開頭的變數會內嵌進 JS bundle，其餘只給 app.config.ts 使用。
# 不需要的變數請整行註解掉，不要留成空值（空字串會覆蓋掉 app.config.ts 的預設值）。

# --- 後端連線（必填）-------------------------------------------------------
# 缺少時 App 讀不到後端：任務牆是空的、登入會失敗、登入頁會顯示「沒有後端連線設定」。
# 網址不要帶結尾斜線，也不要加 /rest/v1。anonKey 是公開金鑰，內嵌進 App 是預期行為。
EXPO_PUBLIC_BILT_URL=
EXPO_PUBLIC_BILT_ANON_KEY=

# --- Android 地圖（選填）---------------------------------------------------
# Google Cloud Console → 啟用 Maps SDK for Android → 建立 API 金鑰。
# 缺少時地圖模式是灰底（不會報錯），任務牆的清單模式不受影響。
# 金鑰若限制成「Android 應用程式」，SHA-1 要填本機簽章憑證的值（見 README 第 14 節）。
# GOOGLE_MAPS_ANDROID_API_KEY=

# --- 套件識別碼與版本（選填）-----------------------------------------------
# 留空會用 app.config.ts 的預設值 com.yourcompany.yourapp（只能裝在測試裝置上）。
# 要上架、或要讓 Google Maps 金鑰的套件名稱限制生效，就必須填成自己的識別碼。
# BILT_ANDROID_PACKAGE=com.example.talentmatch
# BILT_IOS_BUNDLE_ID=com.example.talentmatch
# BILT_APP_VERSION=1.0.0
